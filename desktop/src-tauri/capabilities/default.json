{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the Haven desktop window.",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "opener:default"
  ]
}
